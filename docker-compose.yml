services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: orders-frontend:latest
    ports:
      - '80:80'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - ORDERS_API_URL=http://localhost:8081      
      - ORDERS_ENDPOINT=/orders